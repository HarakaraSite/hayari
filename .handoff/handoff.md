- 2026-07-10 15:48 [codex] docs/tasks.md と docs/yarr-research.md を実装へ同期。次は v1 Phase 0（CI・healthz・version）または SSRF 対策。
- 2026-07-10 15:56 [codex] Phase 0 完了: /healthz、/api/status の Version 貫通、GitHub Actions CI を追加。go test/vet/race は成功。次は SSRF 対策（/page・feed・favicon）。
- 2026-07-10 16:04 [codex] SSRF 対策を実装: safehttp が public HTTP(S) のみ許可し、/page・feed・favicon・リダイレクトを保護。go test/vet/race 成功。次はログインレート制限または HTTP サーバータイムアウト。
- 2026-07-10 16:09 [codex] Web/GReader ログインに IP 単位のレート制限を追加（5 回失敗で15分ロック、成功でリセット）。go test/vet/race 成功。次は HTTP サーバータイムアウト。
- 2026-07-10 16:14 [codex] HTTP server に ReadHeader 10s、Read/Write 30s、Idle 2m、shutdown 10s を設定。go test/vet/race 成功。次は無認証の外部 bind 警告または Cookie Secure/セキュリティヘッダ。
- 2026-07-10 16:24 [codex] レビュー指摘を修正: ログイン失敗 IP 記録に15分TTLと10,000件上限を追加。go test/vet/race 成功。変更一式をコミット予定。
- 2026-07-10 18:50 [codex] 全体レビューの5件を修正: keyset continuation、Server単位token、Cookie username照合、status制約、FTS削除同期。go test/vet/race 成功。コミット後にセキュリティ全体レビュー予定。
- 2026-07-10 19:01 [codex] 実用的セキュリティレビューの残件を修正: 無認証外部bind拒否、Secure Cookie opt-in、feed 5MiB上限、GReader POST既定。go test/vet/race 成功。コミット予定。
- 2026-07-10 20:01 [codex] E2E/実機検証で OPML・Readability・画面幅・既読/スターを確認。サイドバー縦並びCSSと SQLite 単一接続（SQLITE_BUSY 解消）を追加。次は HTTPS リバースプロキシ検証。

## 2026-07-23 22:03 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: UI 縮小とサイト URL からのフィード検出

### 実施したこと
- 基準フォントサイズとデスクトップ・タブレットのカラム幅を 80% に縮小。
- フィード検出に RSS 用 User-Agent、HTTP エラー処理、2 MiB の HTML 読取上限を追加。2NN と animanch.com のベース URL から RSS 候補を実機確認。
- `go test ./...` と `git diff --check` が成功。

### 次のタスク候補
- ブラウザで縮小後の密度とフィード追加フローを確認し、必要なら縮小率を調整。

### 連絡・注意事項
- ローカルテストサーバーは `http://127.0.0.1:17070`、認証は `e2e-user` / `e2e-password`。一時 DB を使用。

## 2026-07-23 22:45 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: 一覧右端揃えとショートカット一覧

### 実施したこと
- 入れ子フィード行を含めて行幅を統一し、未読数と `…` の固定右端位置を揃えた。
- 検索欄のプレースホルダーを削除し、Settings に実装済みキーボードショートカット一覧を追加。
- `node --check`、`go test ./...`、差分チェックを成功させ、ローカルサーバーを再起動。

### 次のタスク候補
- 右端揃えとショートカット一覧の表示密度をブラウザで確認。

### 連絡・注意事項
- テーマが反映されない場合は Settings の保存値を確認する。直前の確認では `light` が保存されていた。

## 2026-07-23 22:48 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: hayari ローカル UI 改善の中間記録
- アプリ名: hayari（流行）

### 実施したこと
- ベース URL からのフィード検出、UI 基準サイズ・可変カラム、フィード／フォルダ操作、フォルダ作成、テーマ・フォント・ダイアログ・未読数表示、検索欄、ショートカット一覧を変更。
- フィード検出は 2NN と animanch.com の実機確認済み。各変更後に `node --check`、`go test ./...`、差分チェックを実施。
- ローカル確認サーバーを `http://127.0.0.1:17070` で起動中。

### 次のタスク候補
- ユーザーから続く UI 改善要望を受け、最新スクリーンショットを基に表示・操作を調整。
- Beige は Settings で選択して Save する。保存値が `light` のままなら Firefox 起因ではない。

### 連絡・注意事項
- 作業ツリーには未コミット変更あり（`.handoff/handoff.md`、assets、crawler と crawler_test）。コミット・push は未実施。
- ローカル確認用 DB は一時領域 `/tmp/hayari-local-test.sTPhEq/`。認証は `e2e-user` / `e2e-password`。

## 2026-07-23 22:41 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: 未読数の右端揃えとテーマ反映確認

### 実施したこと
- 未読数と `…` を右端から固定配置し、フォルダ／インデントされたフィードを含めて数値の右端を揃えた。
- 保存済み設定を確認し、テーマが反映されない原因は Firefox ではなく `theme: light` が保存されていたためと特定。
- `node --check`、`go test ./...`、差分チェックを成功させ、ローカルサーバーを再起動。

### 次のタスク候補
- Settings で Beige を選択して Save し、色味が期待どおりか確認。

### 連絡・注意事項
- ローカルテストサーバーは `http://127.0.0.1:17070`、認証は `e2e-user` / `e2e-password`。一時 DB を使用。

## 2026-07-23 22:35 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: 表示密度とベージュテーマ

### 実施したこと
- ダイアログを本文・最大幅ともに 90% 相当に縮小し、未読数をフィード名の後方で右寄せ。
- Font size 設定を記事本文だけでなくアプリ全体の基準フォントサイズへ適用し、Settings に Beige テーマを追加。
- `node --check`、`go test ./...`、差分チェックを成功させ、ローカルサーバーを再起動。

### 次のタスク候補
- Beige の色味、ダイアログ縮小率、Small/Large の体感をブラウザで確認し、必要なら色値・倍率を微調整。

### 連絡・注意事項
- ローカルテストサーバーは `http://127.0.0.1:17070`、認証は `e2e-user` / `e2e-password`。一時 DB を使用。

## 2026-07-23 22:15 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: リロード操作と可変カラム

### 実施したこと
- リロード操作を `try/finally` 化し、失敗時も busy 表示と無効化を必ず解除。
- デスクトップでサイドバー／記事一覧の境界をドラッグ可能にし、幅を localStorage に保持。基準 UI サイズは 90% に変更。
- `node --check`、`go test ./...`、差分チェック、ローカルの refresh API（202）を確認。

### 次のタスク候補
- ブラウザで 90% の密度とドラッグ幅を確認。必要に応じて縮小率・最小幅を再調整。

### 連絡・注意事項
- ローカルテストサーバーは引き続き `http://127.0.0.1:17070` で稼働中。一時 DB を使用。

## 2026-07-23 22:24 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: フィード一覧の個別操作とフォルダ作成

### 実施したこと
- UI 基準サイズを 100% に復帰し、フィード／フォルダ行の `…` 操作を常時表示。従来の編集ダイアログをそのまま開く。
- サイドバー下部に `+ Folder` を追加し、名前入力ダイアログからフォルダを作成可能にした。
- `node --check`、`go test ./...`、差分チェックを成功させ、ローカルサーバーを再起動。

### 次のタスク候補
- 実データで 100% の表示密度と、フィード行操作／フォルダ作成フローを確認。

### 連絡・注意事項
- ローカルテストサーバーは `http://127.0.0.1:17070`、認証は `e2e-user` / `e2e-password`。一時 DB を使用。

## 2026-07-24 16:00 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: UI reviewer P1/P2 対応
- アプリ名: hayari（流行）

### 実施したこと
- 保存済みカラム幅を現在のビューポートへクランプし、フォルダ選択時は子フィードに active を付けないよう状態更新を分離。
- 未読数を固定幅・右寄せのプレーンテキストへ変更。Refresh の誤った `r` ヒントとキーボード操作不能な区切りのフォーカスを削除。
- Light/Beige のツールバーを高コントラスト化。`node --check`、`go test ./...`、差分チェックを成功させ、ローカルサーバーを再起動。

### 次のタスク候補
- 最新 UI をブラウザ確認。Beige は Settings で選択・Save する（直前の保存値は light）。

### 連絡・注意事項
- Settings 内ショートカット一覧の再追加はユーザー意図に反するため見送り。検索欄はプレースホルダーなしで `title="Search (/)"` のみ保持。

## 2026-07-24 19:26 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: FreshRSS互換同期とUI改善
- アプリ名: hayari（流行）

### 実施したこと
- `/api/greader.php` 別名、10進数記事ID／継続トークン、両方の本文ID形式、必須 `updated` を実装し、ReadKit と NetNewsWire のログイン・同期・`edit-tag` 更新を実機確認した。
- `docs/freshrss-api.md` に入口URL、同期形式、確認済みクライアント、未実装APIを記録した。
- UIでは中央ヘッダーのフィード管理、検索アイコン削除、設定アイコン、テーマ整理、キーボード／更新処理を調整した。

### 次のタスク候補
- 常用環境での同期継続と、未読数表示など保留したUI調整を必要になった時点で再検討する。

### 連絡・注意事項
- ローカルテストサーバーは `http://127.0.0.1:17070` で稼働中。ReadKit診断プロキシは停止済み。

## 2026-07-24 19:58 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari リリース計画と名称移行

### 実施したこと
- `docs/v1-release-plan.md` を更新し、Forgejo の新規リポジトリ `forge.harakara.site/littleisland/hayari`、採用済み v1 要件、対象外、公開・検証・ロールバック手順を保存した。
- Go module path、`cmd/hayari`、ビルド、CI、UI、テスト、文書、既定 DB、User-Agent を Hayari／`hayari` へ統一し、旧表記の残存を除去した。
- `go test ./...`、`go vet ./...`、JavaScript 構文確認、`make build VERSION=v1.0.0-rc.0`、`./hayari --version`、差分チェックを成功させた。

### 次のタスク候補
- 認証情報を安全に渡す起動設定、README 日本語版、LICENSE/CHANGELOG、Forgejo Actions リリース workflow を計画順に実装する。

### 連絡・注意事項
- Forgejo リポジトリ作成、remote 設定、commit、push、タグ、Release は未実施。公開承認後にだけ行う。

## 2026-07-24 20:00 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari Forgejo リポジトリ作成

### 実施したこと
- `fja repo create hayari --force-remote` で private の `littleisland/hayari` を Forgejo に作成した。
- `origin` を `https://forge.harakara.site/littleisland/hayari.git` に設定し、空のリモートであることを確認した。

### 次のタスク候補
- 常用環境で確認できる候補コミットを作成して private リポジトリへ push する。公開設定の変更は確認後に別途行う。

### 連絡・注意事項
- 現在の名称移行・リリース準備の変更は未コミットかつ未 push。リポジトリの public 化、タグ、Release は未実施。

## 2026-07-24 20:02 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 初回 private push

### 実施したこと
- Hayari 名称移行とリリース計画を commit `63aa075`（`Rename application to Hayari`）にまとめ、private の `origin/main` へ push した。
- ローカルと `origin/main` の SHA が `63aa075149d6bd48d57c376c5d232af30f55a045` で一致することを確認した。

### 次のタスク候補
- private 常用環境で候補を確認し、認証設定、公開用ドキュメント、Forgejo Actions リリース workflow を計画順に実装する。

### 連絡・注意事項
- リポジトリの public 化、タグ、Release は未実施。常用確認後に明示的な判断を得る。

## 2026-07-24 22:24 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari post-release 修正メモ

### 実施したこと
- 常用画面の観察を `docs/post-release-followups.md` に記録した。未読数の右端揃え、favicon 表示、検索ヒット漏れ、記事本文リンクの新規タブ表示を分離している。

### 次のタスク候補
- 検索漏れの再現ケースを最初に採取し、次にサイドバー表示と記事本文リンクを小さな独立変更として実装・検証する。

### 連絡・注意事項
- この記録ではコードを変更していない。検索問題は再現条件が未確定のため、FTS や UI の原因を断定しない。

## 2026-07-25 14:25 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari favicon と記事本文リンク

### 実施したこと
- 埋め込みの `/favicon.svg` を追加し、ログイン画面を含む HTML から参照した。favicon は認証なしで取得できる。
- 記事本文を表示した直後に、本文リンクへ新規タブと `noopener noreferrer` を付与する処理を追加した。
- `node --check`、`go test ./...`、`go vet ./...`、差分チェックを成功させた。

### 次のタスク候補
- ローカル起動後に、ブラウザタブの favicon と記事本文リンクが新規タブで開くことを実機確認する。

### 連絡・注意事項
- 既存の post-release 修正メモと合わせて未コミット。未読数・検索の変更はまだ実装していない。

## 2026-07-25 14:29 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 印章風 favicon

### 実施したこと
- favicon を角丸の印章風アウトラインと「流」の SVG に更新し、ログイン後のサイドバー見出しで Hayari の左にも同じマークを表示した。
- `node --check`、`go test ./...`、`go vet ./...`、差分チェックを成功させた。

### 次のタスク候補
- ローカル起動後に小さい favicon とサイドバー表示を実機確認する。

### 連絡・注意事項
- favicon・本文リンク新規タブ・修正メモを含めて未コミット。未読数・検索の変更はまだ実装していない。

## 2026-07-25 14:38 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 日本語検索のヒット漏れ調査

### 実施したこと
- あにまんch の `All` で、タイトル `【ハンターハンター】` に対する検索語 `ハンター` が 0 件となる再現を確認した。
- SQLite FTS5 の `MATCH` は日本語の部分一致を行わず、完全語と前方一致のみが一致することを最小 DB で確認し、修正メモに記録した。

### 次のタスク候補
- 検索仕様（タイトルのみか本文も含むか、任意部分一致を必須にするか）を決め、再現テストを先に追加して検索方式を修正する。

### 連絡・注意事項
- 調査・文書化のみ。検索実装、未読数の変更はまだ行っていない。

## 2026-07-25 15:04 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari タイトル専用の日本語検索

### 実施したこと
- 検索索引を本文なし・タイトル専用の FTS5 trigram 索引へ移行し、3文字以上の日本語部分一致と1〜2文字のタイトル限定フォールバックを実装した。
- 旧 DB の索引再構築、本文だけの語句が不一致、`ハンター` の一致をテストし、`go test`・`go vet`・差分チェックを成功させた。

### 次のタスク候補
- 実データを持つ環境で、あにまんch の `ハンター` 検索と短い検索語を確認する。

### 連絡・注意事項
- localhost:7070 は `/tmp/hayari-preview.db` を使う更新版プレビュー。今回までの favicon・記事リンク・検索変更は未コミット。

## 2026-07-25 15:12 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 将来のフィード非表示フィルタ

### 実施したこと
- Web UI 管理、フィード単位、タイトルのリテラル部分一致、新規記事の完全非表示、Google Reader API 対象外という将来仕様を修正メモへ記録した。

### 次のタスク候補
- 実装を始める際に、既存の `/api/filters` をこの仕様へ置換するためのデータモデル・API・UI・既存記事の扱いを設計する。

### 連絡・注意事項
- 記録のみ。フィルタの動作・API・UI は変更していない。

## 2026-07-25 15:16 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 未読数バッジの整列

### 実施したこと
- 未読数バッジを固定幅・等幅数字へ変更し、1,000 件以上を `999+` と表示するようにした。フォルダ合計と既読・未読更新時も同じ表示規則を使う。
- `node --check`、`go test ./...`、`go vet ./...`、差分チェックを成功させた。

### 次のタスク候補
- harakara の実データで、フィード・フォルダ・ネスト済みフィードの未読バッジが揃うことを視覚確認する。

### 連絡・注意事項
- 0 件フィード・空フォルダの Unread 表示からの非表示は、今回の対象外として未実装。変更は未コミット。

## 2026-07-25 15:32 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari 未読数バッジの Grid 修正と DOM 検証

### 実施したこと
- favicon 欠落時に Grid 自動配置がずれる問題を、アイコン・名前・バッジの列明示指定で修正した。Pico CSS のネストリスト余白も上書きした。
- Playwright CLI でフォルダと `3`・`47`・`999`・`999+` の各バッジが同じ左端 208px、右端 251px、幅 43px となることを確認した。

### 次のタスク候補
- なし

### 連絡・注意事項
- `/tmp/hayari-unread-preview.db` を使う localhost:7070 のプレビューは起動中。今回までの変更は未コミット。

## 2026-07-25 15:42 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari Unread の0件フィード非表示

### 実施したこと
- Unread では未読0件フィードと未読の子がないフォルダを隠し、既読・未読更新、タブ切替、キーボード移動対象、選択中の対象が消えた場合の Unread 全体への復帰を同じ規則で実装した。
- Playwright で Unread には0件フィード・空フォルダがなく、All には両方が復帰することを確認した。

### 次のタスク候補
- なし

### 連絡・注意事項
- `/tmp/hayari-unread-preview.db` を使う localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 15:53 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari Starred の空フィード・空フォルダ非表示

### 実施したこと
- `/api/stats` にフィード別スター件数を追加し、Starred ではスター付き記事0件のフィードと該当する子がないフォルダを非表示にした。All は従来どおり全件表示する。
- 最後のスターを外して選択元が消えた場合は Starred 全体へ戻る。Go テスト・vet と Playwright DOM 検証を完了し、検証用スターも復元した。

### 次のタスク候補
- なし

### 連絡・注意事項
- `/tmp/hayari-unread-preview.db` を使う localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 16:02 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari Starred の件数バッジ

### 実施したこと
- サイドバーの件数を表示中タブに合わせ、Unread は未読数、Starred はスター数、All はバッジなしに変更した。フォルダ合計も同じ規則で集計する。
- Playwright DOM で Starred の各バッジが `starred items`、All のバッジ数が0であることを確認した。

### 次のタスク候補
- なし

### 連絡・注意事項
- `/tmp/hayari-unread-preview.db` を使う localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 16:14 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari フィード単位タイトル非表示キーワード

### 実施したこと
- フィード編集ダイアログにカンマ区切りキーワード欄を追加し、フィード設定として保存する DB マイグレーションと API 更新を実装した。空白・空要素・重複を正規化する。
- 取り込み時に記事タイトルのリテラル部分一致を OR で判定し、一致した新規記事を保存しない。Go テスト・vet・Playwright の入力欄確認を完了した。

### 次のタスク候補
- 既存記事にフィルタを遡及適用するかは別機能として検討する。

### 連絡・注意事項
- `/api/filters` の既存正規表現ルールとは別の、フィード設定として実装している。`/tmp/hayari-unread-preview.db` を使う localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 16:25 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari タイトル非表示フィルタの既存記事適用と追加後再表示

### 実施したこと
- キーワード保存時に既存記事を `hidden` 状態へ再評価し、一覧・検索・GReader 同期の取得対象から除外するようにした。キーワードを外せば再表示する。旧DBも移行時に一度再評価する。
- 入力欄のキーワード例 placeholder を削除し、フィード追加後は初回取得を最大5秒待って新しいフィードを選択・再読込するようにした。

### 次のタスク候補
- なし

### 連絡・注意事項
- Playwright で あにまんch の `/api/items` にハンター記事が含まれないことを確認済み。localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 16:51 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari reviewer 指摘の整合性修正

### 実施したこと
- 追加フィードはサーバー側の初回取得完了後に応答するようにし、UI の5秒ポーリングを削除した。非表示になるフィードは All を選択してから開く。
- 記事作成トランザクション内で最新のタイトルキーワードを読むようにし、既存正規表現フィルタの `hide` も完全非表示へ統一した。既読更新は API 成功後に UI へ反映する。

### 次のタスク候補
- セキュリティ reviewer の hidden 記事 GReader 全経路専用回帰テスト不足は、ユーザー判断で受容。

### 連絡・注意事項
- `go test ./...`、`go vet ./...`、`node --check`、`git diff --check` が成功。localhost:7070 のプレビューは起動中。変更は未コミット。

## 2026-07-25 17:08 JST

- 実行エージェント: Codex
- モデル: 不明
- 作業トピック: Hayari v1.1.1 リリース

### 実施したこと
- README に登録済みフィードを表示した実画面のスクリーンショットを追加し、タイトル非表示フィルタを含む変更を `v1.1.0` として push した。
- Forgejo runner の展開済みモジュールキャッシュ不整合で `v1.1.0` の macOS arm64 ビルドが失敗したため、リリースビルドだけ空の `GOMODCACHE` を使うよう修正した。
- Go 1.25.0 で6配布対象をビルドし、`v1.1.1` を push。Forgejo Actions run 8 の build-and-release 成功を確認した。

### 次のタスク候補
- harakara の稼働環境を `v1.1.1` へ更新する場合は、既存DBをそのまま使ってバックアップ後にバイナリを差し替える。

### 連絡・注意事項
- `v1.1.0` の失敗済みタグは履歴として残している。利用・配布対象は成功済みの `v1.1.1`。
